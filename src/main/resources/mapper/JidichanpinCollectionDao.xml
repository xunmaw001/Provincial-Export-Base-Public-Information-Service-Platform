<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.JidichanpinCollectionDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.jidichanpin_id as jidichanpinId
        ,a.yonghu_id as yonghuId
        ,a.insert_time as insertTime
        ,a.create_time as createTime
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.JidichanpinCollectionView" >
        SELECT
        <include refid="Base_Column_List" />

--         级联表的字段
        ,jidichanpin.jidichanpin_name as jidichanpinName
        ,jidichanpin.jidichanpin_types as jidichanpinTypes
        ,jidichanpin.jidichanpin_photo as jidichanpinPhoto
        ,jidichanpin.jidichanpin_content as jidichanpinContent
        ,yonghu.yonghu_name as yonghuName
        ,yonghu.sex_types as sexTypes
        ,yonghu.yonghu_id_number as yonghuIdNumber
        ,yonghu.yonghu_phone as yonghuPhone
        ,yonghu.yonghu_photo as yonghuPhoto
        ,yonghu.role as role

        FROM jidichanpin_collection  a
        left JOIN jidichanpin jidichanpin ON a.jidichanpin_id = jidichanpin.id
        left JOIN yonghu yonghu ON a.yonghu_id = yonghu.id

        <where>
            <if test="params.jidichanpinId != null">
                and a.jidichanpin_id = #{params.jidichanpinId}
            </if>
            <if test="params.yonghuId != null">
                and a.yonghu_id = #{params.yonghuId}
            </if>
            <if test=" params.insertTimeStart != '' and params.insertTimeStart != null ">
                <![CDATA[  and DATE_FORMAT(a.insert_time, '%Y-%m-%d %H:%T:%s') >=
                DATE_FORMAT(#{params.insertTimeStart}, '%Y-%m-%d %H:%T:%s')   ]]>
            </if>
            <if test=" params.insertTimeEnd != '' and params.insertTimeEnd != null ">
                <![CDATA[  and DATE_FORMAT(a.insert_time, '%Y-%m-%d %H:%T:%s') <=
                DATE_FORMAT(#{params.insertTimeEnd} , '%Y-%m-%d %H:%T:%s')    ]]>
            </if>

            <if test=" params.jidichanpinName != '' and params.jidichanpinName != null and params.jidichanpinName != 'null' ">
                and jidichanpin.jidichanpin_name like CONCAT('%',#{params.jidichanpinName},'%')
            </if>
            <if test="params.jidichanpinTypes != null">
                and jidichanpin.jidichanpin_types = #{params.jidichanpinTypes}
            </if>

            <if test=" params.jidichanpinContent != '' and params.jidichanpinContent != null and params.jidichanpinContent != 'null' ">
                and jidichanpin.jidichanpin_content like CONCAT('%',#{params.jidichanpinContent},'%')
            </if>
            <if test=" params.yonghuName != '' and params.yonghuName != null and params.yonghuName != 'null' ">
                and yonghu.yonghu_name like CONCAT('%',#{params.yonghuName},'%')
            </if>
            <if test="params.sexTypes != null">
                and yonghu.sex_types = #{params.sexTypes}
            </if>

            <if test=" params.yonghuIdNumber != '' and params.yonghuIdNumber != null and params.yonghuIdNumber != 'null' ">
                and yonghu.yonghu_id_number like CONCAT('%',#{params.yonghuIdNumber},'%')
            </if>
            <if test=" params.yonghuPhone != '' and params.yonghuPhone != null and params.yonghuPhone != 'null' ">
                and yonghu.yonghu_phone like CONCAT('%',#{params.yonghuPhone},'%')
            </if>
            <if test=" params.role != '' and params.role != null and params.role != 'null' ">
                and yonghu.role like CONCAT('%',#{params.role},'%')
            </if>
        </where>
        order by a.${params.orderBy} desc
    </select>

</mapper>